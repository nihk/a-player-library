package library.mediaplayer

import android.media.MediaPlayer
import library.common.TrackInfo

internal fun MediaPlayer.setTrackInfo(trackInfo: TrackInfo) {
    // todo
}

internal fun MediaPlayer.clearTrackInfos(index: Int) {
    // todo
}

internal fun MediaPlayer.getTrackInfos(trackType: Int): List<TrackInfo> {
    // todo
    return emptyList()
}